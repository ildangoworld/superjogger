# SuperJogger 관리자 사이트 요구사항

> 관리자 사이트의 제품 요구사항과 구현 기준이다. 사용자 서비스의 기준은 `README.md`이며, 이 문서와 충돌하면 사용자 서비스 원칙(개인정보 최소 노출 등)이 우선한다. 진행 상태는 `PROGRESS.md`에 기록한다.

## 1. 원칙

1. 관리자는 운영에 필요한 최소 정보만 본다. 통증 상세, 개인 메모 전문, AI 분석 본문은 관리자 화면에 기본 노출하지 않는다.
2. 관리자 권한 검증은 서버에서만 수행한다. 클라이언트 판단을 신뢰하지 않는다.
3. 관리자용 데이터 조회는 서버 전용(service role) 경로로만 수행하고, 사용자 테이블의 RLS는 완화하지 않는다.
4. AI API 키와 서비스 역할 키는 환경변수에만 둔다. 관리자 화면에서 키를 조회·수정할 수 없다.
5. 파괴적 조치(회원 탈퇴 처리, 크루 삭제, 문서 게시)는 확인 절차를 거치고 처리자·시각을 기록한다.

## 2. 정보 구조 (메뉴)

| Depth 1 | Depth 2 | 권한 키 |
|---------|---------|---------|
| 대시보드 | 지표 요약, AI 사용 현황 | `dashboard` |
| 회원 관리 | 회원 목록, 회원 상세 | `members` |
| 크루 관리 | 크루 목록, 크루 상세 | `crews` |
| 문의 관리 | 문의 목록, 문의 상세·답변 | `inquiries` |
| 콘텐츠 관리 | 이용약관, 개인정보처리방침 | `legal` |
| 설정 | AI 설정, 관리자 계정 관리, 내 계정(비밀번호 변경) | `settings` |

- 사이드바 메뉴는 접기/펼치기가 가능하다. 데스크톱은 아이콘+라벨 사이드바, 모바일은 햄버거 버튼으로 여닫는 오버레이 방식으로 동작한다.
- Depth 1 메뉴는 권한 키 단위로 노출을 제어한다. 권한이 없는 메뉴는 사이드바에서 숨기고, URL 직접 접근 시 서버에서 차단한다.
- `내 계정(비밀번호 변경)`은 모든 관리자에게 항상 노출한다.

## 3. 관리자 계정과 권한

### 3.1 데이터 모델

- `admin_users`
  - `user_id uuid PK FK auth.users` — Supabase Auth 계정을 재사용한다
  - `role text not null` (`SUPER`, `STAFF`)
  - `permissions text[] not null` — depth 1 권한 키 배열
  - `created_at`, `updated_at`
- `SUPER`는 permissions와 무관하게 전체 메뉴에 접근하고, 관리자 계정 관리(추가·권한 변경·비활성화)는 `SUPER`만 수행한다.

### 3.2 인증·보호

- 관리자 로그인은 기존 Supabase 이메일 로그인을 사용하되, `/admin` 진입 시 서버에서 `admin_users` 존재 여부를 확인한다.
- `src/proxy.ts`에서 `/admin` 경로를 보호 라우트로 추가한다. 미로그인은 `/admin/login`, 비관리자는 403 화면으로 보낸다.
- 페이지·서버 액션 양쪽에서 권한 키를 재검증한다.

### 3.3 내 계정

- 로그인한 관리자는 자신의 비밀번호를 변경할 수 있다(현재 비밀번호 확인 후 `auth.updateUser`).

## 4. 화면별 요구사항

### 4.1 대시보드 (`dashboard`)

- 지표 카드: 총 회원 수, 오늘·최근 7일 신규 가입, 오늘 기록된 운동 수, 최근 7일 기록 사용자 수
- AI 사용 현황: 오늘 분석 호출 수(성공/실패), 오늘 한도(3회) 도달 사용자 수
- 최근 가입 회원 5명, 최근 문의 5건 바로가기

### 4.2 회원 관리 (`members`)

- 목록: 닉네임·이메일·가입일·온보딩 완료·마지막 운동일, 닉네임/이메일 검색
- 상세: 프로필, 현재 주간 목표와 진행, 조거 등급, 누적 기록 수, AI 사용량(오늘/누적), 가입 크루, 동의한 약관 버전
- 조치: 회원 탈퇴 처리(확인 후 `auth.admin.deleteUser`, 연관 데이터는 기존 CASCADE 정책을 따름)
- 비노출: 통증 상세, 메모 전문, AI 분석 본문, 개별 운동의 건강 수치

### 4.3 크루 관리 (`crews`)

- 목록: 이름·소유자 닉네임·멤버 수·생성일, 이름 검색
- 상세: 멤버 목록(닉네임·역할·가입일), 초대 코드 재발급, 크루 삭제(운영 이슈 대응)

### 4.4 문의 관리 (`inquiries`)

- 목록: 상태(답변 대기/답변 완료) 필터, 제목·작성자·작성일
- 상세: 문의 본문, 답변 작성·수정, 답변 시 상태 자동 전환과 답변자·답변시각 기록

### 4.5 콘텐츠 관리 (`legal`) — 이용약관·개인정보처리방침

법적 근거에 따라 버전과 이력을 관리한다.

- **이용약관** — 약관의 규제에 관한 법률 제3조(명시·교부), 전자상거래법 준용
  - 개정 시 시행일과 개정 사유를 명시하고 시행일 이전에 서비스 내 공지한다. 사용자에게 불리한 변경은 시행 30일 전, 그 외는 7일 전 게시를 기본값으로 한다.
  - 기능: 초안 작성 → 시행일 지정 게시(예약), 게시 후 수정 불가(새 버전으로 개정), 전체 개정 이력 열람
- **개인정보처리방침** — 개인정보 보호법 제30조(수립·공개 의무)
  - 필수 기재사항(처리 목적, 항목, 보유 기간, 제3자 제공, 위탁, 파기, 정보주체 권리, 안전성 확보 조치, 보호책임자 등)을 갖춘다.
  - 변경 시 지체 없이 공개하고, 이전 버전을 계속 열람할 수 있게 유지한다.
  - 기능: 버전 게시, 사용자 공개 페이지에서 이전 버전 목록 제공

데이터 모델:

- `legal_documents`
  - `id uuid PK`, `doc_type text` (`TERMS`, `PRIVACY`), `version integer`
  - `content text`(마크다운), `status text` (`DRAFT`, `PUBLISHED`, `ARCHIVED`)
  - `effective_date date`, `change_summary text`, `published_by uuid`, `published_at`
  - unique `(doc_type, version)`
- `user_consents`
  - `id uuid PK`, `user_id`, `doc_type`, `version`, `consented_at`
  - 회원가입 시 필수 동의 체크와 함께 현재 게시 버전을 기록한다.

초기 콘텐츠 초안은 `docs/legal/terms-of-service.md`, `docs/legal/privacy-policy.md`에 있으며, 마이그레이션 시드로 버전 1을 게시한다. 사업자 정보 등 `[운영자 입력 필요]` 표기는 게시 전에 채운다.

### 4.6 설정 (`settings`)

- **AI 설정**: `app_settings` 테이블 기반으로 관리자가 수정한다.
  - `ai_model`(모델명), `ai_daily_limit`(일일 분석 한도, 기본 3), `ai_base_url`
  - API 키는 환경변수 유지. 서버 분석 파이프라인은 env보다 `app_settings` 값을 우선 사용한다.
- **관리자 계정 관리** (`SUPER` 전용): 관리자 추가(기존 회원 이메일 지정), 역할·메뉴 권한 변경, 관리자 해제
- **내 계정**: 비밀번호 변경

데이터 모델:

- `app_settings`: `key text PK`, `value jsonb`, `updated_by uuid`, `updated_at`

## 5. 사용자 사이트 추가 사항

1. 공개 페이지 `/terms`, `/privacy` — 게시된 최신 버전 렌더링, 이전 버전 목록 링크. 로그인 없이 접근 가능.
2. 회원가입 화면에 이용약관·개인정보처리방침 필수 동의 체크박스를 추가하고, 가입 시 `user_consents`에 문서 버전과 함께 기록한다.
3. 프로필(마이페이지)에 `문의하기` 섹션 추가 — 문의 작성, 내 문의 목록과 답변 확인.
   - `inquiries`: `id`, `user_id`, `title`, `content`, `status` (`OPEN`, `ANSWERED`, `CLOSED`), `answer_content`, `answered_by`, `answered_at`, `created_at`
   - RLS: 사용자는 자신의 문의만 조회·작성. 답변 작성은 관리자 서버 경로에서만.
4. `/welcome`과 인증 화면 하단에 약관·방침 링크를 노출한다.

## 6. 기술 방침

- 라우트: `src/app/admin/` (사용자용 `(app)` 셸과 분리된 전용 레이아웃)
- 레이아웃: 접이식 사이드바(데스크톱 고정, 모바일 오버레이), 상단에 현재 메뉴 경로 표시
- 데이터 접근: 관리자 페이지·액션은 서버에서 `admin_users` 검증 후 `createServiceRoleClient()`로 조회한다. 새 테이블(`admin_users`, `app_settings`, `legal_documents`, `user_consents`, `inquiries`)에는 RLS를 적용하되 관리자 쓰기는 service role 경로로만 수행한다.
- DB 변경은 migration 파일로 관리한다.

## 7. 구현 순서

### Phase A1. 관리자 기반

- `admin_users` 마이그레이션, `/admin` 보호 라우트, 관리자 로그인/403
- 접이식 사이드바 셸, 권한 키 기반 메뉴 노출
- 내 계정 비밀번호 변경

### Phase A2. 대시보드·회원·크루

- 대시보드 지표, 회원 목록/상세/탈퇴 처리, 크루 목록/상세/삭제·코드 재발급

### Phase A3. 약관·개인정보처리방침

- `legal_documents`, `user_consents` 마이그레이션 + 버전 1 시드
- 사용자 `/terms`, `/privacy` 공개 페이지, 가입 동의 기록
- 관리자 문서 버전 관리(초안·예약 게시·이력)

### Phase A4. 문의하기

- `inquiries` 마이그레이션, 프로필 문의 작성/목록, 관리자 목록/답변

### Phase A5. 설정·품질

- `app_settings` + AI 설정 화면, 분석 파이프라인 연동
- 관리자 계정 관리(`SUPER`), 전체 권한·모바일 접힘 동작 점검

## 8. 필수 테스트 시나리오

- 비관리자 계정으로 `/admin` URL 접근 시 서버에서 차단
- 권한 키가 없는 관리자에게 해당 메뉴가 숨겨지고 직접 접근도 차단
- `SUPER`가 아닌 관리자는 관리자 계정 관리에 접근 불가
- 게시된 약관은 수정 불가, 새 버전 게시 시 이전 버전이 이력에 유지
- 가입 시 동의 버전이 기록되고 회원 상세에서 확인 가능
- 사용자는 자신의 문의만 조회 가능, 타인 문의 접근 불가
- AI 일일 한도를 설정에서 변경하면 분석 슬롯 검증에 반영
- 관리자 화면 어디에도 통증 상세·메모 전문·AI 분석 본문이 노출되지 않음
